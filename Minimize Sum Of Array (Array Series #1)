function minSum(arr) {
  arr = arr.sort((a, b)=> a - b).map((item, index) => {
    return item = arr[index] * arr[arr.length - (1 + index)]
    }).reduce((acc, item)=> acc += item,0)
  return arr / 2;
}
