function findMissing(arr1, arr2) {
  arr1.map((item, index) => {
    if (arr2.includes(item)) {
      arr1.splice(index,1, " ");
      arr2.splice(arr2.indexOf(item),1);
    }
  })
  return +arr1.join(' ');
}
